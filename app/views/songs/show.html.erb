<h1><%= @song.title %> </h1>
<p><%= link_to "Edit Song", edit_song_path(@song) %></p>
<p><%= link_to "Delete Song", song_path(@song), method: :delete, data: {confirm: "Really delete the song?"} %></p>
  <p><%= link_to @artist.name, artist_path(@artist)%></p>
  <p><%= link_to "All Songs", songs_path%></p>
