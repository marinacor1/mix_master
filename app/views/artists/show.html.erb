<h1><%= @artist.name %> </h1>
<%= image_tag @artist.image_path %>
<p><%= link_to "Edit Artist", edit_artist_path(@artist) %></p>
<%= link_to "Delete Artist", artist_path(@artist), method: :delete, data: {confirm: "Really delete the artist?"} %>
<%= link_to "New song", new_artist_song_path(@artist) %>
